import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse(`{"title":"","description":"","frontmatter":{"layout":"home","hero":{"name":"Laravel Error Audit","text":"Your logs, read for you.","tagline":"An AI-assisted audit of yesterday's errors and warnings, in your inbox before you open the editor.","image":{"src":"/audit-mail.png","alt":"The daily error audit email"},"actions":[{"theme":"brand","text":"Get Started","link":"/quick-start"},{"theme":"alt","text":"View on GitHub","link":"https://github.com/aaix/laravel-error-audit"}]}},"headers":[],"relativePath":"index.md","filePath":"index.md","lastUpdated":null}`);
const _sfc_main = { name: "index.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">composer</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> require</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> aaix/laravel-error-audit</span></span></code></pre></div><h2 id="why-not-sentry" tabindex="-1">Why not Sentry? <a class="header-anchor" href="#why-not-sentry" aria-label="Permalink to &quot;Why not Sentry?&quot;">​</a></h2><p>Sentry, Kibana and their peers are built for live monitoring at scale — large rollouts where audit volume is high and real time visibility genuinely matters. Most applications are not that. They log to a file, the file grows, and nobody reads it until something is already on fire.</p><p>For those applications a built-in daily summary is simpler, cheaper and entirely sufficient. That is what this package is.</p><h2 id="what-lands-in-your-inbox" tabindex="-1">What lands in your inbox <a class="header-anchor" href="#what-lands-in-your-inbox" aria-label="Permalink to &quot;What lands in your inbox&quot;">​</a></h2><p><strong>A status word and four numbers.</strong> Errors and warnings with their change since the last report, how many distinct issue types occurred, and how many of them are new. The subject line carries the same signal, so you know the answer from the lock screen.</p><p><strong>A timeline.</strong> A spike at 03:00 tells a different story than an even spread or a wall starting at 14:20. Errors and warnings stacked by issue type, rendered to an image so it shows in every mail client.</p><p><strong>One card per issue, sorted by urgency.</strong> A plain-language title, the likely cause, and a suggestion for confirming it — read from your own source code where the failure occurred, not guessed from a stack trace you decode over coffee.</p><h2 id="what-it-costs" tabindex="-1">What it costs <a class="header-anchor" href="#what-it-costs" aria-label="Permalink to &quot;What it costs&quot;">​</a></h2><p>Each distinct issue is analysed <strong>once</strong> and cached by fingerprint. Ten thousand occurrences of the same exception are one request. An issue already assessed yesterday is free. A typical daily report costs a fraction of a cent, and the exact figure is printed at the bottom of every email.</p><h2 id="what-leaves-your-application" tabindex="-1">What leaves your application <a class="header-anchor" href="#what-leaves-your-application" aria-label="Permalink to &quot;What leaves your application&quot;">​</a></h2><p>The log lines, and — by default — the source files named in each stack trace, so the model can read the code instead of guessing. Everything is redacted first, and the redaction layer cannot be switched off. Look at the exact payload before you enable anything:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">php</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> artisan</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> error-audit:preview</span></span></code></pre></div><p>No API call is made. What you see is what would be sent. Source files put your own code in that payload, so review it — and turn it off with <code>ERROR_AUDIT_INCLUDE_SOURCE=false</code> if your environment forbids it.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("index.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const index = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  index as default
};
